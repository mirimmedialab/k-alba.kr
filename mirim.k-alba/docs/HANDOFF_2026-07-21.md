# K-ALBA 작업 인수인계 (2026-07-21)

> 다른 PC에서 이어서 작업할 때: Claude 데스크톱 앱에서 새 Cowork 세션을 열고
> 이 파일을 첨부한 뒤 "이 문서 읽고 [하고 싶은 작업] 이어서 진행해줘" 라고 하면 됩니다.

## 0. 오늘(7/21) 요약 — 표준근로계약서 PDF 완성

단시간근로자 표준근로계약서(고용노동부 서식) PDF 자동 생성이 완료·배포되었습니다.
모든 변경은 **운영 Supabase 엣지 함수에 직접 배포**되어 이미 라이브 상태입니다(별도 퍼블리싱 불필요).

| 항목 | 상태 |
|---|---|
| 엣지 함수 `fill-contract-form` | **v16 배포 완료(운영)** — 아래 소스 첨부 |
| 클라이언트 `lib/contractStandardPdf.js` | 커밋 `2f0c066` (운영 main) 배포 완료 |
| 운영 DB `contracts` 컬럼 | `contract_start`·`contract_end`·`rest_day` 모두 존재(확인 완료) |

### v9 → v16 변경 이력 (모두 운영 반영)
- 근로계약기간(종료일) 추가: 기간제 계약은 "…부터 …까지" 렌더링 (+ 클라가 end_y/m/d 전달)
- 요일 헤더 `( )요일` 빈칸에 근무요일 자동 기입
- 4대보험 체크를 박스 정중앙으로 정렬
- **전 항목 baseline 재정렬**: 근무장소·업무내용·주휴일·임금(시급/가산율/지급일)·서명란(날짜·사업체명·전화·주소·대표자·연락처·성명)이 인쇄 라벨과 같은 기준선에 오도록 보정
- 상여금/기타급여 "없음" 및 직접지급 체크 위치 보정
- 인쇄된 "(서명)" 글자를 흰색으로 덮고 **실제 서명 이미지를 크게** 배치(우측 여백 활용)
- 사업주 주소를 콜론 뒤로 우측 이동

### 남은 확인(사용자 직접) 1건
- 운영 사이트(www.k-alba.kr)에서 **로그인 후** 계약 상세 → 계약서 PDF 다운로드로 최종 육안 확인.
  (Claude는 보안상 로그인 비밀번호 입력 불가 — 로그인만 해주시면 이후 확인은 도와드릴 수 있음)

## 1. 프로젝트 자원

| 항목 | 값 |
|---|---|
| 코드 저장소 | github.com/mirimmedialab/k-alba.kr (public) · 운영 브랜치 **main** · 앱 경로 `mirim.k-alba/` |
| 운영 사이트 | www.k-alba.kr (미림미디어랩 Vercel — 이 계정 MCP로는 조회 불가, GitHub 푸시로 자동배포) |
| 운영 Supabase | ref `uqgqqsescalotabaivee` (엣지 함수·양식 Storage·비자 데이터) |
| 엣지 함수(운영) | fill-contract-form **v16**, fill-partwork-form v10, visa-chatbot v4, kakao-visa-bot v2 |
| 계약서 양식 원본 | Storage `forms/contract-form-1p-c4.pdf` (595×842pt) |
| 확인서 양식 원본 | Storage `forms/form.pdf` (v4) |
| 카카오 채널 | KALBA 외국인 알바 (@kalba) · 오픈빌더 봇 "K-ALBA 비자 도우미"(kooly5 계정, 준비 완료) |

> 참고: 이 프로젝트의 엣지 함수는 git으로 추적하지 않고 Supabase에 직접 배포합니다.
> 다른 PC에서 함수 소스가 필요하면 Supabase MCP `get_edge_function`(function_slug)으로 최신본을 받으면 됩니다.
> 편의를 위해 fill-contract-form v16 전체 소스를 이 폴더의 `fill-contract-form.v16.ts` 로 함께 보관합니다.

## 2. 남은 작업 (우선순위)

1. **카카오 오픈빌더 마무리** (약 5분, 절차 검증 완료 · 블로커: 계정 접근)
   - @kalba에 연결된 기존 챗봇 소유 카카오 계정으로 chatbot.kakao.com 로그인
   - 그 봇 → 스킬 목록에 `https://uqgqqsescalotabaivee.supabase.co/functions/v1/kakao-visa-bot` 등록
   - 폴백 블록 → "스킬데이터" 응답 연결 → 배포 → 실채널 발화 테스트
2. **시간제취업 확인서(partwork) + 대학담당자(staff) 오픈** — 코드 완성, 실데이터 리허설 1회 + 파일럿 대학 담당자 1명 섭외
3. **contracts 관련 운영 점검** — rest_day 컬럼은 운영 DB에 이미 존재(확인). 조건수정 챗봇 "주휴일" 메뉴 동작 최종 확인
4. **이메일 캠페인** — 발신 도메인/SMTP 설정, 수신동의 필터, 소량 테스트 발송
5. **출입국 통합신청서(immigration)** — 신청 진입 플로우 설계(작업량 큼, 장기)

## 3. 다른 PC에서 세션 시작하는 법

1. Claude 데스크톱 앱에서 새 Cowork 세션 시작 (Supabase·GitHub·크롬 커넥터 연결 확인)
2. 이 파일(HANDOFF_2026-07-21.md)을 대화에 첨부
3. "이 문서 읽고 [예: 카카오 오픈빌더 마무리 / partwork 리허설] 이어서 진행해줘"
- 카카오 작업: 오픈빌더 로그인만 사람이 직접, 화면 조작은 Claude에게 맡기면 됨(크롬 확장 연결 필요)
- 코드 작업: 저장소 public이라 Claude가 클론·분석 가능, 커밋은 GitHub 로그인된 브라우저(웹 업로드)로 진행
- 엣지 함수 수정: Supabase MCP `get_edge_function`으로 현재 소스 확보 → 수정 → `deploy_edge_function`

## 4. 주의사항
- Vercel 자동배포는 GitHub main 푸시 기준. 미림미디어랩 Vercel은 이 계정 MCP로 조회 불가(GitHub Deployments 탭에서 상태 확인).
- 엣지 함수는 운영에 직접 배포되므로 배포 즉시 라이브. 되돌리려면 이전 버전 소스로 재배포.
- 기존 Google Doc 인수인계(2026-07-21)에는 함수 버전이 옛 값으로 적혀 있음 → 실제 운영은 **fill-contract-form v16**.
