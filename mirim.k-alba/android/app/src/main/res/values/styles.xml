<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Android 15(타깃 SDK 35) edge-to-edge 강제 해제 →
             웹뷰 콘텐츠가 상태바(시계·배터리) 아래에서 시작하도록 함 (겹침 방지) -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- 상태바: 네이비 배경 + 흰색 아이콘 -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <!-- 런치(스플래시) 테마 -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+(API 31+) 시스템 스플래시: 네이비 배경 + 투명 아이콘(로고 숨김).
             실제 로고/애니메이션은 웹 오버레이(AppSplash)에서만 보여준다. -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_screen_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Android 11 이하(레거시) 스플래시 -->
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
